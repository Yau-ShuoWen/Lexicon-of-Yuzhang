<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.shuowen.yuzong.dao.mapper.Character.NamNotationMapper">

    <select id="selectMdrDialectChars" resultType="com.shuowen.yuzong.dao.model.Character.MdrDialectChar">
        select
        mdr.id     h_id,
        hp_comb,
        m          left_id,
        n          right_id,
        main.id    d_id,
        main.hanzi hanzi,
        hantz,
        std_py
        from mdr_char mdr
        RIGHT JOIN NC.nam_char_mdr map ON mdr.id = map.m
        RIGHT JOIN NC.nam_char main ON map.n = main.id
        WHERE hp_comb IN
        <foreach collection="list" item="hpComb" open="(" separator="," close=")">
            #{hpComb}
        </foreach>
        GROUP BY m, n
    </select>

</mapper>